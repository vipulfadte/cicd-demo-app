apiVersion: v1
kind: Service
metadata:
  namespace: area51
  name: cicd-demo-app-service
spec:
  selector:
    app: cicd-demo-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      #nodePort: 30081
  type: LoadBalancer
  externalIPs:
    - 10.0.0.110